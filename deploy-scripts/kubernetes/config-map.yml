apiVersion: v1
kind: ConfigMap
metadata:
  name: https-redirect
data:
  redirect.conf: |
    server {
      listen 80;
      server_name guyrking.com;
      if ($http_x_forwarded_proto = "http") {
        return 200 https://$host$request_uri;
      }
    }
